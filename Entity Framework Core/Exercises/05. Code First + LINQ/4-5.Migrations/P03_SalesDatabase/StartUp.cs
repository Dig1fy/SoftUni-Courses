using P03_SalesDatabase.Data;
using P03_SalesDatabase.Data.Seeding;
using P03_SalesDatabase.Data.Seeding.Contracts;
using P03_SalesDatabase.IOManagement;
using P03_SalesDatabase.IOManagement.Contracts;
using System;
using System.Collections.Generic;

namespace P03_SalesDatabase
{
    public class StartUp
    {
        static void Main()
        {
            // WE SHOULD HAVE ONLY 1 PROJECT! Otherwise, Judge will throw errors. 

            /*
            ******The following code is for the Seeder Task. 
            */

            //var salesContext = new SalesContext();
            //var random = new Random();
            //var consoleWriter = new ConsoleWriter();
            

            //ICollection<ISeeder> seeders = new List<ISeeder>();
            //seeders.Add(new ProductSeeder(salesContext, random, consoleWriter));
            //seeders.Add(new StoreSeeder(salesContext, consoleWriter));

            //foreach (var seeder in seeders)
            //{
            //    seeder.Seed();
            //}




        }
    }
}
